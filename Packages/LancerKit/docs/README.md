# LancerKit Notes

This folder only holds package-local notes. Current launch state lives in the repo-level docs:

- `README.md` for repository orientation
- `ARCHITECTURE.md` for product and module architecture
- `docs/PUBLISH_READINESS_CHECKLIST.md` for release gates
- `docs/KNOWN_ISSUES.md` for active audit findings

The old M2/M3 milestone fragments were converted to historical notes on 2026-06-17 because the current code and tests supersede them.
