# 08 — Findings Register

Lancer iOS — full-audit, 2026-06-28. Engineering axes only (UI/UX/design axis skipped per owner).
Every finding below was **verified by reading the cited code**, not taken from a subagent at face value.
Status: `OPEN` (carry to Phase B) · `VERIFIED-SAFE` (challenged → not a real issue) · `NOTE` (informational/monitor).

| ID | Severity | Category | Finding | Evidence | Impact | Recommended action | Status |
|----|----------|----------|---------|----------|--------|--------------------|--------|
| SEC-2 | **Medium** | Security / Reliability | Cold-launch relay-token hydration is fire-and-forget; the backend-relay fast path reads an empty `relayToken` before hydration runs. | `ApprovalRelay.swift:270-291` (`hydrateCredentialsIfNeeded` spawns a `Task`, no await) → `:187-188` calls it then immediately `await postDecisionToBackend` → `:306` `guard !relayToken.isEmpty` short-circuits *before* the URLSession await `:314`. Comment `:267-269` asserts safety — incorrect. | On a true cold-launch approval (no live SSH channel, no e2e bridge) the first forward is queued instead of relayed; decision still durable in DB+audit, recovered via SSH-drain or lancerd 120s timeout. Defeats the Phase-1 cold-launch optimisation it was built for. | Make `hydrateCredentialsIfNeeded()` `async`; `await` it before `postDecisionToBackend` (and in `enqueue`). Add the regression test (TEST-01). | OPEN |
| CONC-2 | **Medium** | Concurrency / Crash | `sendRPC` write-failure `catch` resumes the checked continuation **unconditionally** after `removeValue`. A concurrent `failPendingRPCs` (disconnect) can resume+remove the same continuation first → double-resume → fatal. | `DaemonChannel.swift:92-95` catch resumes unconditionally; `:47`→`:66-71` `failPendingRPCs` resumes+removes all; `:52-60` `handleFrame` is safe (nil-checked remove-then-resume). Write-failure and session-death are correlated. | Hard crash (`CHECK FAILED` on checked continuation) when an in-flight RPC's write throws at the same moment the SSH session drops. | Guard the catch resume: `if pendingRPC.removeValue(forKey: id) != nil { cont.resume(throwing: error) }`. Add unit test (cancel mid-RPC). | OPEN |
| TEST-01 | Medium | Testing | No test covers the cold-launch `relayToken`-empty forwarding path (the #1 product gate). | `ApprovalRelayBackendTests.swift` covers JSON body only; no hydration-race / empty-token / 401 / queue-then-drain test. | Regressions in the approval loop ship undetected. | Add Swift Testing case asserting the Bearer header is populated before the POST (pairs with SEC-2 fix). | OPEN |
| TEST-02 | Medium | Testing | No round-trip test for session resume / follow-up `runId` extraction & validation. | `AgentResumeBuilderTests` covers argv only; `RunControlStore` continue path has no test for malformed/empty `runId`. | A daemon returning empty `runId` breaks approval matching on follow-up turns, untested. | Add `continueRun` tests: valid runId parsed; empty runId rejected with typed error. | OPEN |
| ARCH-1 | Low | Maintainability / Build | `AppRoot.mainBody` is a 165-line modifier chain (7×`.task`, 3×`.onChange`, 7×`.onReceive`); 380ms type-check — the sole build warning. | `AppFeature/AppRoot.swift:314` (build warning), chain `:314-479`. | Slow incremental builds / IDE; hard to test individual lifecycle hooks. | Extract 3 grouped modifier blocks into computed properties (`setupTasks`/`lifecycleObservers`/`notificationSubscriptions`); recompose. No behaviour change. | OPEN |
| CONC-1 | Low | Concurrency | Detached 3s timeout `Task` in `awaitUnifiedShellReady` is never stored/cancelled (uses `weak self`, so no crash, just an orphaned task per connect cycle). | `SessionViewModel.swift:1010-1013`. | Minor task accumulation over many connect/disconnect cycles. | Store the task; cancel in `closeUnifiedShell()`. | OPEN |
| SEC-1 | Low | Security (defense-in-depth) | `lancer://` deep-link handler validates scheme+host but not path/params. | `Lancer/LancerApp.swift:59-76`. Token parsing is server-validated (Supabase), so no current bypass. | Future handlers could be reached by smuggled paths; violates secure-defaults. MASVS MSTG-PLATFORM-1. | Tighten guard to expected `host`+`path`; ignore unexpected params. | OPEN |
| TEST-03 | Low | Testing | Flaky-prone fixed `Task.sleep`/`usleep` waits instead of polling. | `ReconnectTests.swift:111-114`; `TapInjectionProofTests.swift:47,71,136,142,218`. | Intermittent CI failures under load. | Replace fixed sleeps with bounded poll-until-condition. | OPEN |
| BUILD-1 | Low | Build / Release | CI runs Xcode 26.0/26.0.1 while local + project pin is Xcode 27.0 / Swift 6.2. | `.github/workflows/ci.yml` (Xcode select) vs `project.yml`. Both currently compile clean. | Compiler/strict-concurrency drift could pass locally, fail (or vice-versa) on CI. | Align CI to Xcode 27. 2-min change. | OPEN |
| BUILD-2 | Low | Config | `LANCER_RELAY_URL` env override is **not** behind `#if DEBUG` (intentional self-hoster feature) and is unvalidated. | `RelaySettings.swift:31`. All *other* `LANCER_*` seams are correctly `#if DEBUG` (verified — see 04/06). | A bad env value silently redirects the relay endpoint (fails closed on bad connect). | Decide: keep as a documented self-host feature with URL validation, or gate to DEBUG. | OPEN |
| TEST-04 | Low | Reliability | `ApprovalRelay.queue` (un-forwarded decisions) is in-memory only; lost on process death before drain. | `ApprovalRelay.swift:67,193`. Decision itself is durable (DB+audit `:108-140`); lancerd 120s auto-deny is the fail-safe backstop. | A persisted-but-unforwarded *approve* falls back to auto-deny if the process dies before next attach. Fail-safe (denies, never wrongly approves). | Optional: re-drive undelivered decisions from DB on `setChannel`. Document the backstop. | OPEN |
| TEST-INFRA | Medium | Testing infra | iOS-gated package tests (`#if os(iOS)` in `LancerKitTests`) run nowhere: `swift test`/CI use the macOS host (which skips them), and the package's iOS test build fails to compile. Discovered while running TEST-01 in Phase B. | `HostControlKit/HostServiceClient.swift:60,69` use `homeDirectoryForCurrentUser` (unavailable on iOS) → `xcodebuild test -scheme LancerKit-Package -destination 'iOS Simulator'` fails. Affects the pre-existing `ApprovalRelayBackendTests` too. | The approval-loop unit tests that matter most are effectively unrun; regressions ship undetected. | Guard HostControlKit macOS-only code `#if os(macOS)`, or move iOS-gated tests to an iOS-buildable test target; then wire into CI. | OPEN |
| SEC-3 | Low | Supply chain | `swift-nio-ssh` pinned to community fork `github.com/Wellz26/swift-nio-ssh` @ `a05e6bbe…` (0.3.6), not Apple upstream. | `Packages/LancerKit/Package.resolved`. Known/accepted (Mac Catalyst + certs patch; awaiting Citadel upstream migration). | Security patches depend on a single third party. MASVS MSTG-CODE-1. | Add a quarterly drift-check vs `apple/swift-nio-ssh`; migrate when Citadel supports upstream. | NOTE |
| CQ-1 | Low | Code quality | Guarded force-unwrap reads as a crash risk but is safe. | `SettingsFeature/SettingsView.swift:666-668` (`if auditRepository != nil { … auditRepository! }`). | None (cannot crash). Style only. | Replace with `if let auditRepository`. | OPEN |
| CQ-2 | Info | Code quality | Stale doc comment: `DSButton.primary` described as white-on-X but renders the accent (orange), same as `.accent`. | `DesignSystem/Components/DSButton.swift`. | Misleading to future readers (already caused a real white-button bug historically). | Correct the comment (or collapse the duplicate case). | OPEN |
| CQ-3 | Info | Code quality | Silent `try?` on persistence writes drops errors with no log on a few paths. | `AppRoot.swift:421,426,1315,1331`. | Harder to diagnose lost run-output/policy-save; no data-loss beyond the write. | Log on failure (no behaviour change); surface a toast for user-initiated saves. | OPEN |
| CQ-4 | Info | Hygiene | Candidate unused `import DiffFeature` in AppRoot. | `AppRoot.swift:19` (subagent-reported; **verify with a reference grep before removing** — DiffFeature types may be used transitively). | Trivial. | Confirm unused, then remove. | OPEN |
| SEC-VERIFIED | — | Security | Strong baseline confirmed: device-only Keychain (`whenUnlocked…ThisDeviceOnly`, `synchronizable=false`), fail-closed TOFU + BiometricGate (passcode fallback, no silent degrade), two-tier relay auth with constant-time compare + production fail-closed, compliant `PrivacyInfo.xcprivacy`, no hardcoded secrets, comprehensive `Redactor`, WebKit preview restricted to localhost/`lancer-preview://`, pasteboard carries no secrets, `.private` logging on pairing code/token. | See 04. | — | None. | VERIFIED-SAFE |
| ACCT-476 | — | Concurrency/Crash (challenged) | Reported "risky force-cast" `EmptyResponse() as! Response` is **gated** by `Response.self == EmptyResponse.self` on the same line — cannot fail. | `AccountKit/AccountClient.swift:476`. | None. | No action (optional: protocol-default to drop the `as!`). | VERIFIED-SAFE |
| URL-STALE | — | Config (challenged) | Reported "stale `conduit-push` URL" is **intentional and live** — the Cloud Run service name was deliberately preserved across the Conduit→Lancer rebrand so pairing keeps working. | `project.yml:26`; confirmed in `PUBLISH_READINESS_CHECKLIST.md`, `LIVE_LOOP_RUNBOOK.md`, `lancer-infra-migration-checklist.md`. | None. | No code change. Cosmetic rename only as part of a planned infra migration. | VERIFIED-SAFE |
| DEBUG-SEAMS | — | Release safety | All dev/mock/test seams (`LANCER_DESTINATION/SEED_DEMO/FAKE_RELAY_HOST/FORCE_PRO/DRAWER_OPEN/RELAY_CODE/PUSH_BACKEND_URL/UITEST_RESEED/TEST_*`, `DebugSeeder`) verified `#if DEBUG`-gated. Only `LANCER_RELAY_URL` is intentionally not (BUILD-2). | See 06. | — | None (except BUILD-2). | VERIFIED-SAFE |

## Severity tally (actionable)
- **Critical:** 0
- **High:** 0
- **Medium:** 4 (SEC-2, CONC-2, TEST-01, TEST-02)
- **Low:** 7 · **Info:** 4 · **Verified-safe / challenged:** 5

The two "priority-1 crash bugs" from the initial sweep (ACCT-476, SettingsView force-unwrap) were both
disproved on inspection — a direct result of the *verify-before-recording* rule.
