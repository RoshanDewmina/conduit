# Agent changelog

Append-only log of agent-made changes. **Every agent session that lands a change (commit,
PR, deploy, doc edit, config/infra change) appends exactly one line per landed unit of work
BEFORE reporting done** — newest first, under today's date heading. One line: what changed
and why, with the branch/PR/commit as the pointer. No essays — the diff is the detail; this
file exists so humans and future agents can see at a glance what has been happening without
re-deriving it from git archaeology. Do not rewrite or delete old lines.

Format: `- HH:MM <agent> — <what + why> (<branch or PR link>)`

## 2026-07-18

- 19:45 Claude Fable (orchestrator) — pi adapter (d): approval extension (tool_call veto via lancerd agent-hook, loaded per-run via -e at realLauncher choke point), installer + docs/pi-lancer-gate.ts; pi stays fail-closed in hookWiredForAgent until model-driven veto is live-fire verified (OpenRouter credits ran out — 402). Also FIX: requiresLancerGate now covers codex/kimi — their LANCER_GATE-guarded hook scripts would otherwise silently no-op on dispatched runs (fail-open once trusted) (branch claude/vendor-cli-parity-multi-account-209d7c)
- 19:50 Claude Fable (orchestrator) — pi adapter (e): doctor checkAgentCLIs includes pi; new pi-extension doctor check (installed-but-unverified wording matches kimi's) (branch claude/vendor-cli-parity-multi-account-209d7c)

- 20:15 Claude Sonnet 5 — Phase 3(c): Pi continue/resume argv. `continueArgv`'s "pi" case builds `pi --continue --mode json [--provider][--model] -p <prompt>` (most-recent-session-in-cwd, "latestInCwdFallback" resume mode); `resumeArgv`'s "pi" case builds `pi --session <id> --mode json [--provider][--model] -p <prompt>` (exact session, "exact" resume mode) — never `-r/--resume` (interactive picker, hangs headless). `buildConversationArgv` needed no pi-specific changes — it already routes generically once agentArgv/continueArgv/resumeArgv all return ok=true for "pi". Live-verified 2026-07-18: a `--session <id>` resume reused the same session id and recalled prior-turn context (scratchpad/pi-smoke/pi-resume.jsonl, already captured for Phase 3's kickoff); a fresh `--continue` run in a HOME scoped to one prior session correctly resumed that exact session id (scratchpad/pi-smoke/pi-continue-stream2.jsonl line 0 `id` matches the prior session) before hitting an unrelated OpenRouter 402 credits error on the model call itself — a billing issue, not a resume-mechanics failure. `go build ./... && go vet ./... && go test ./...` clean from daemon/lancerd (branch claude/vendor-cli-parity-multi-account-209d7c)
- 19:55 Claude Sonnet 5 — Phase 3(b): Pi launch argv + `--mode json` stream parsing. `agentArgv`'s new "pi" case builds `pi --mode json [--provider P] [--model M] -p <prompt>` (splitPiModel splits a "provider/model-id" string; a bare model with no "/" passes through as --model only) — no `-e <path>` extension flag yet (Phase 3(d)). `streamJSONOutput` gained pi cases: "session" captures the vendor session id (the `id` field, not `sessionID`/`sessionId`); "message_update" maps `assistantMessageEvent.type` thinking_start/thinking_delta → emitLiveStatusThinking, text_delta → emitLiveStatusStreaming + forwarded chunk (using the `delta` field, concatenated across deltas — NOT the cumulative `partial` text), toolcall_end → emitToolArtifact (pi hands back the complete resolved {id,name,arguments}, no delta-accumulation needed unlike Claude's input_json_delta path); tool_execution_start/update/end are suppressed as redundant with toolcall_end. Live-verified 2026-07-18 against pi 0.80.10 with a bash-tool-triggering prompt (`Run 'echo hello-pi-tool-test' using your bash tool...`) — captured raw stream at scratchpad/pi-smoke/pi-tool-stream.jsonl, fixture tests in pi_dispatch_test.go pin exact lines from that capture (thinking_start→thinking, toolcall_start/delta/end→one tool.start with the resolved args, text_delta sequence→"okok" concatenation) plus a negative test (unknown top-level and assistantMessageEvent types parse to nothing). `go build ./... && go vet ./... && go test ./...` clean from daemon/lancerd (branch claude/vendor-cli-parity-multi-account-209d7c)
- 19:30 Claude Sonnet 5 — Phase 3(a): new Pi vendor adapter, session reader. `pi_session_reader.go` parses `~/.pi/agent/sessions/<sanitized-cwd>/<ISO-ts>_<sessionId>.jsonl` into neutral SessionInfo/SessionMessage (mirrors codex_session_reader.go/kimi_session_reader.go); `piSanitizeCWD`'s dash-padding algorithm reverse-engineered from live directory names (`/` and `.` → `-`, then pad to a `--` prefix/suffix) — verified live 2026-07-18 against pi 0.80.10 (captured fixtures: scratchpad/pi-smoke/). Handles user/assistant text, assistant `thinking` and `toolCall` content parts, and `role:"toolResult"` messages (union confirmed against a live `pi --mode json` run with a bash tool call, not just research-repos/pi's types.ts, which is MIT/Copyright 2025 Mario Zechner — patterns only referenced in comments, no verbatim code copied). Unknown entry/content types parse to nil, never error (TestPiMessageEntryUnknownTypeDoesNotError). Registered `pi` in normalizeAgentSource and wired piSessions/piTranscript into session_index.go's discovery + transcript-fallback chain (after codex, since pi session ids are also bare UUIDs). `go build ./... && go vet ./... && go test ./...` clean from daemon/lancerd (branch claude/vendor-cli-parity-multi-account-209d7c)
- 19:05 Claude Sonnet 5 — Phase 2 live-status parity in dispatch.go: codex agentArgv/continueArgv/resumeArgv now add `-c model_reasoning_summary=auto` and opencode adds `--thinking`, unlocking reasoning events on both streams (verified live 2026-07-18 against codex-cli 0.144.6 and opencode 1.17.18 — with/without smoke pairs in daemon/lancerd/dispatch_livestatus_parity_test.go's doc comments); streamJSONOutput now maps codex `item.completed{item.type=="reasoning"}` and opencode `{"type":"reasoning"}` to emitLiveStatusThinking, and closes the kimi gap (previously raw-text-only, no emitLiveStatus* calls at all) by wiring emitLiveStatusStreaming for assistant text (both the pre-existing flat `{role,content}` shape and a new `context.append_message` case matching kimi_session_reader.go's already-proven wire.jsonl shape) plus emitLiveStatusTool for toolCalls — kimi's live stream-json shape itself remains NOT live-verified (kimi CLI still 402s on membership) so no thinking state was invented for it. `go build ./... && go vet ./... && go test ./...` clean from daemon/lancerd (branch claude/vendor-cli-parity-multi-account-209d7c)
- 18:22 Claude Sonnet 5 — correction to the 17:05 entry below: re-checked ~/.codex/config.toml directly and the existing conduit-hook.sh trust record reads `[hooks.state."/Users/roshansilva/.codex/hooks.json:pre_tool_use:0:0"]` with `enabled = false`, not "trusted+working" — the conduit PreToolUse hook is currently NOT firing on this machine. Live-verified (see entry above): an untrusted/disabled hook is silently skipped by Codex, so this matters for anyone relying on that hook today. Flagging per AGENTS.md's stale-claim rule rather than repeating it forward
- 18:20 Claude Sonnet 5 — Codex per-action approval hooks closed the loop: `codex_hook_install.go` wires ~/.codex/hooks.json AND checks the config.toml [hooks.state] trust record (not just JSON presence — an untrusted hook is silently skipped by Codex); `hookWiredForAgent` now returns true for codex once trusted. Live-verified against codex-cli 0.144.6 in an isolated CODEX_HOME: untrusted hook was skipped (file write proceeded), `--dangerously-bypass-hook-trust` proved the hook fires and fails closed when lancerd is unreachable. Also fixed a fail-open bug found in the existing docs/codex-lancer-hook.sh draft (auto-approved when lancerd was missing) and added the missing LANCER_GATE guard so an owner's ordinary interactive Codex session is never routed through Lancer. Landed `kimi_hook_install.go` (install/doctor-only — kimi stays fail-closed in hookWiredForAgent per the standing decision, since `kimi --output-format stream-json` still 402s on membership) and added kimi to doctor's checkAgentCLIs + two new doctor checks (codex trust state, kimi installed-but-unverified). New docs/kimi-hooks.json + docs/kimi-lancer-hook.sh mirror the Codex canonical copies (branch claude/vendor-cli-parity-multi-account-209d7c)
- 17:05 Claude Fable (orchestrator) — vendor-cli-matrix refreshed against live binaries (claude 2.1.214, codex 0.135.0, opencode 1.17.18, kimi 0.18.0, pi not installed); found Codex+Kimi now ship Claude-style PreToolUse hooks.json (trusted+working on this machine) and Kimi local default_permission_mode=yolo (branch claude/vendor-cli-parity-multi-account-209d7c)

## 2026-07-17

- 16:50 Claude Fable (orchestrator) — LancerWidgets signing fixed: Xcode had NO Apple ID signed in (owner action, one-time); once signed in, App Groups capability auto-registered and all 3 signing errors cleared. Debug device build now SUCCEEDS with LancerWidgets.appex embedded, installed+launched on owner's physical iPhone (both Lancer + LancerWidgets processes confirmed running)
- 16:57 Claude Fable (orchestrator) — TestFlight build 3 UPLOADED (delivery `b942714a`, tip `aaf265ad`) — first build to include widgets/Live Activity extension
- 17:20 Claude Sonnet 5 — iOS 27 Siri/App Intents research + ranked feature ideas written (docs/product/2026-07-17-ios27-siri-opportunities.md, branch docs/ios27-siri-ideas); key finding: deep LongRunningIntent path already shipped in code, unverified live; App Schemas has no matching Apple domain yet
