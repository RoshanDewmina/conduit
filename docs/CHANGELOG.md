# Agent changelog

Append-only log of agent-made changes. **Every agent session that lands a change (commit,
PR, deploy, doc edit, config/infra change) appends exactly one line per landed unit of work
BEFORE reporting done** — newest first, under today's date heading. One line: what changed
and why, with the branch/PR/commit as the pointer. No essays — the diff is the detail; this
file exists so humans and future agents can see at a glance what has been happening without
re-deriving it from git archaeology. Do not rewrite or delete old lines.

Format: `- HH:MM <agent> — <what + why> (<branch or PR link>)`

## 2026-07-18

- 22:23 Cursor Grok — closed Goal 1 Live Activity visual gap on sim (`lease-222`): Lock Screen LA banner + Island indicator captured while `Activity.request succeeded` (`F593D5C9-…`); IslandCapture UITest waits 25s foreground before Home; SET-failure alert still not obtained (daemon-kill UITest failed + watchdog); accidental `lancerd pair --help` rotated prod code to `190799` — phone needs re-pair; evidence in `docs/test-runs/2026-07-18-live-activity-sim/` (uncommitted worktree)
- 20:50 Cursor Composer — Goal 2 (device-build): failed-cwd UserDefaults store (`dev.lancer.failedCwds`) + `preferredDefaultRepo`/`defaultRepo` recency-first composer default (skips stale high-count missing host paths); ShellLiveBridge surfaces `cwd does not exist` with pick-another-folder copy; unit tests in `WorkspaceRepoCatalogTests` (uncommitted worktree)
- 19:25 Claude Fable 5 — independent reviewer (fresh-context Sonnet subagent) caught 2 major + 2 minor issues in the Live Activity wiring and this session fixed all four: (1) production path could never set `pendingApprovalID` on the activity (Approve/Reject buttons never rendered — the exact MAJOR-14 regression the surrounding code exists to prevent) — `RelayApprovalIngest` now threads the most-recent pending approval's ID through `updatePendingApprovals`; (2) an approval arriving before its run's activity existed was cached-then-dropped, permanently under-reporting the count — `LancerLiveActivityManager` now keeps a fleet-wide `pendingApprovals`/ID/risk cache that seeds a freshly-started activity; (3) `DevicePushRegistrationCoordinator`'s single `lastActivityToken` slot let a per-session token silently overwrite the push-to-start token, breaking remote-start-after-closed on daemon restart — split into two independently-tracked slots; (4) documented the `ConnectionStateStore` observer/Task cleanup gap in `deinit`. Re-verified: 20/20 iOS-sim tests green, SPM + device builds green, `registerDevice`/`device registered for push` re-confirmed live on the physical iPhone after rebuild (uncommitted worktree)
- 19:05 Claude Fable 5 — root-caused and fixed APNs app-closed push registration never firing: at cold launch the APNs token beat BOTH coordinator triggers (fire-and-forget cache write not yet landed when `start()` hydrated; `.lancerAPNSTokenReceived` posted before the `for await` listener subscribed — missed forever). Fix: synchronous block-based NotificationCenter observers registered in `start()`, AppDelegate posts only AFTER the cache write, `currentAPNSToken()` re-checks the cache on every registration attempt, `waitForBridgeActive` real 3s deadline, per-connect-epoch send dedup. Live-proven on physical iPhone: `registerDevice` sent, daemon `e2e: device registered for push (apnsToken=true)`, `~/push-device.json` written (note: daemon persists it at `$HOME/push-device.json`, not `~/.lancer/`), app-closed escalation POSTed to conduit-push.fly.dev `/approval` with no rejection. All 8 `DevicePushRegistrationCoordinatorTests` now RUN and pass on iOS sim (previously compile-only — one test reproduced the exact production race). Visual lock-screen banner confirmation pending owner (uncommitted worktree)
- 19:05 Claude Fable 5 — reviewed + hardened Grok's Live Activity wiring: added field-preserving `LancerLiveActivityManager.updateStatus` so ShellLiveBridge status updates no longer clobber `pendingApprovals`/`pendingApprovalID` pushed by `RelayApprovalIngest` (would have hidden the lock-screen Approve/Reject buttons mid-run, the MAJOR-14 failure mode); made `liveActivityStatus(for:)` nonisolated so its tests compile; converted LANCER-DIAG prints to `os.Logger`. 20/20 tests green on iOS sim (`DevicePushRegistrationCoordinatorTests` + `ShellLiveBridge` suites), SPM + device builds green. Live Dynamic Island appearance pending owner dispatching a run (uncommitted worktree)

- 18:15 Cursor Grok — wired Live Activity start/update/end into production `ShellLiveBridge` + pending-count into `RelayApprovalIngest` (was only called from dead SSH-era `SessionViewModel`); Dynamic Island / Lock Screen now tracks relay-dispatched runs (uncommitted worktree)
- 14:38 Claude Sonnet 5 — rebuilt + installed master tip (`69a6f490`) on owner's physical iPhone via an isolated worktree (`/Volumes/LancerDev/worktrees/lancer/device-build`), then re-paired the relay identity (owner's prior pairing had expired after the morning's SSD-migration daemon restart) to resume the App Store device-testing pass (uncommitted worktree)
- 16:15 Claude Sonnet 5 — fixed two bugs found live-dogfooding an observed desktop session on-device: (1) mid-turn queued messages (Claude Code's `type:"attachment"`/`queued_command` JSONL record) were silently dropped by `parseClaudeLine`, never reaching the ledger — now render as a real user turn, 2 regression tests added, `go test ./...` green; (2) no working indicator while watching a live observed session (`sendState` never reflected observed-follow activity) — added `ShellLiveBridge.isObservedSessionWorking`, debounced via `LivePollPolicy.observedFollowIdleGracePolls`. Rebuilt + reinstalled on-device to verify (plain `swift build` doesn't touch either file — both `#if os(iOS)`-gated). See `docs/KNOWN_ISSUES.md` §7 (uncommitted worktree)
- 16:20 Claude Sonnet 5 — opened `docs/KNOWN_ISSUES.md` §7 as a rolling log for bugs found during this device-testing session, per owner request (uncommitted worktree)
- 20:29 Claude Sonnet 5 — verified real approval escalate→approve round-trip live via `lancerd agent-hook` (audit log hash-chained correctly, survived phone reconnects mid-flight)
- 21:45 Claude Sonnet 5 — merged and verified three fixes produced by background agents this session, all confirmed on the real iOS target (XcodeBuildMCP sim build/test, not just SPM) before install: (1) daemon-side `resumeObservedSession` reservation guard preventing double-launch of an observed-session resume, plus client-side `isSendInFlight`/`isObservedSessionWorking` fold so follow-ups queue instead of racing a busy observed session; (2) `DevicePushRegistrationCoordinator` closing the APNs/Live-Activity device-token dead-end (token captured, never forwarded to the daemon — root cause of app-closed push never arriving, live-reproduced this session); (3) two-bug widget/database fix — `AppDatabase.openShared()` was resolving the shared GRDB database inside the *calling process's private sandbox container* instead of the App Group, so Lock Screen/Live Activity approve-reject decisions (which run in the widget extension's own process) silently wrote to a database the main app never saw, plus `RelayApprovalIngest.handle()` never refreshed the widget snapshot on arrival. All three rebuilt + installed on owner's physical iPhone. See `docs/KNOWN_ISSUES.md` §7 for full root-cause writeups (uncommitted worktree)
- 21:50 Claude Sonnet 5 — built `~/bin/claude-account`, a personal (non-Lancer) Keychain-based Claude Code account switcher for the owner's Mac, mirroring Orca's exact mechanism against the real `Claude Code-credentials` Keychain item — not part of this repo
- 21:55 Claude Sonnet 5 — researched (3 parallel agents: Lancer's own vendor-adapter gap matrix, Orca's account/usage patterns, fresh OpenCode+Pi CLI clones) and wrote `docs/plans/2026-07-18-vendor-parity-and-accounts-fable-brief.md` — a paste-ready Fable brief for full Codex/OpenCode/Kimi/Pi adapter parity + an Orca-inspired manual account-switcher view, phased and gated per swarm-orchestrator/lancer-verification-gate; corrected the owner's belief that Orca has automatic/load-balancing account switching — confirmed via two independent broad searches that it does not, only manual switching exists

## 2026-07-17

- 16:50 Claude Fable (orchestrator) — LancerWidgets signing fixed: Xcode had NO Apple ID signed in (owner action, one-time); once signed in, App Groups capability auto-registered and all 3 signing errors cleared. Debug device build now SUCCEEDS with LancerWidgets.appex embedded, installed+launched on owner's physical iPhone (both Lancer + LancerWidgets processes confirmed running)
- 16:57 Claude Fable (orchestrator) — TestFlight build 3 UPLOADED (delivery `b942714a`, tip `aaf265ad`) — first build to include widgets/Live Activity extension
- 17:20 Claude Sonnet 5 — iOS 27 Siri/App Intents research + ranked feature ideas written (docs/product/2026-07-17-ios27-siri-opportunities.md, branch docs/ios27-siri-ideas); key finding: deep LongRunningIntent path already shipped in code, unverified live; App Schemas has no matching Apple domain yet
