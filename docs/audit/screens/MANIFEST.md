# Conduit Debug Gallery — Visual Inventory Manifest

Pre-App-Store UX audit. Screenshots captured from `DebugGalleryView` routes in the iOS Simulator
(iPhone 17 Pro, `095F8B3A-FEA3-4031-A2A5-561755740730`, bundle `dev.conduit.mobile`).
Build: `/tmp/conduit-ga-dd/Build/Products/Debug-iphonesimulator/Conduit.app`.

Route selected via `CONDUIT_GALLERY=<route>`. Light + dark captured per route.
Anomaly notes are lightweight (from `ui_describe_all` text snapshots); the coordinator does the full vision review.

| Route | Light | Dark | Status | Anomaly notes |
|-------|-------|------|--------|---------------|
| _capturing…_ | | | | |
