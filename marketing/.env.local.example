# Backend URL for the Cloud Run billing service (set by WS-5).
# Copy this file to .env.local and fill in the real URL before deploying.
# NEVER use the NEXT_PUBLIC_ prefix — this variable is server-only.
BACKEND_URL=https://lancer-push-HASH-ts.a.run.app
